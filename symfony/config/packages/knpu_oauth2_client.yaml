knpu_oauth2_client:
    clients:
        google:
            type: google
            client_id: '%env(GOOGLE_CLIENT_ID)%'
            client_secret: '%env(GOOGLE_CLIENT_SECRET)%'
            # No usamos redirect, porque esto NO es login web
            redirect_route: null
            redirect_params: {  }
