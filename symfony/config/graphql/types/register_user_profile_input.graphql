"Input para datos del perfil dentro de la mutación register"
input UserProfileInput {
  firstName: String!
  lastName: String!
  companyRole: String
  displayName: String
  description: String
  imageId: ID # Opcional: si querés asociar una Image existente
}
