<form method="post" action="{{ path('backend_store_schedule_delete', {'id': store_schedule.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ store_schedule.id) }}">
    <button class="btn">{{ 'ui.button.delete'|trans }}</button>
</form>
